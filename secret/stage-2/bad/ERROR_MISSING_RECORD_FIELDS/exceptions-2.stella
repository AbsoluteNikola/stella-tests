language core;
extend with
  #structural-subtyping,
  #exceptions,
  #exception-type-declaration,
  #records;

exception type = { a : Nat, b : Nat }

fn main(a : Nat) -> Nat {
  return throw({ a = 0 })
}
